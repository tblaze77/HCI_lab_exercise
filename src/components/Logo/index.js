import React from 'react'
import styles from './style.module.css'

const Logo = () => (
  <section className={styles.logo}>
    <span className={styles.logoText}>Venice Beach</span>
    <span className={styles.sub}>CALIFORNIA</span>
  </section>
)

export default Logo